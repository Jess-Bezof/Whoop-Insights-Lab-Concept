I will update the backend logic to match the refined "Direct HRV Hit" requirements for meals.

### Backend Updates (`backend/main.py`)

1. **Modify** **`/calculate`** **Endpoint Logic**:

   * **Sleep Performance ($S$)**: Keep existing logic (Base quantity + 5%/hr meal penalty + Stress penalty).

   * **Recovery ($R$)**:

     * Keep the base ($S$) and Systemic Stress penalty (-15%/-5%).

### Verification

* I will verify the new logic using a curl command, specifically testing the 3-hour threshold for the flat meal penalty.

