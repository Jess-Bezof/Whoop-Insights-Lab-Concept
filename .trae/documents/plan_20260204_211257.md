I will implement the persistent 2x2 Insight Grid with the specific thresholds and messages provided.

### Backend Updates (`backend/main.py`)

1.  **Modify** **`/calculate`** **Endpoint**:
    *   **Logic Refactor**: Instead of a dynamic `notifications` list, generate a structured `insight_grid` dictionary with 4 fixed keys:
        *   `neuro_toxicity`: { status: "green"|"yellow"|"red", message: "...", value: ... }
        *   `metabolic_state`: { status: "green"|"yellow"|"red", message: "...", value: ... }
        *   `cns_stimulants`: { status: "green"|"yellow"|"red", message: "...", value: ... }
        *   `autonomic_load`: { status: "green"|"yellow"|"red", message: "...", value: ... }
    *   **Thresholds**:
        *   **Neuro-Toxicity (Alcohol)**: Green (0), Yellow (0.1-4.0), Red (>4.0).
        *   **Metabolic State (Food)**: Green (<100), Yellow (100-400), Red (>400).
        *   **CNS Stimulants (Caffeine)**: Green (<20), Yellow (20-100), Red (>100).
        *   **Autonomic Load (Stress)**: Green (Low), Yellow (Med), Red (High).
    *   **Return Object**: Include `insight_grid` in the response.

### Frontend Updates (`frontend/src/App.jsx`)

1.  **Remove Dynamic Notifications**: Replace the "Daily Health Insights" list with a fixed 2x2 grid.
2.  **Create Insight Card Component** (inline or new file):
    *   Props: `title`, `data` (status, message).
    *   **Styling**:
        *   Green: `bg-green-900/20 border-green-500/50 text-green-200`. Icon: Checkmark.
        *   Yellow: `bg-yellow-900/20 border-yellow-500/50 text-yellow-200`. Icon: Warning Triangle.
        *   Red: `bg-red-900/20 border-red-500/50 text-red-200`. Icon: Lightning/Alert.
    *   **Layout**: 2x2 Grid using CSS Grid (`grid-cols-2`).

### Verification
*   I will verify with a test case:
    *   Active Alcohol: 3.0 (Yellow).
    *   Active Calories: 50 (Green).
    *   Caffeine: 120 (Red).
    *   Stress: Low (Green).
    *   Result: Grid should show 4 cards with correct colors and messages.

