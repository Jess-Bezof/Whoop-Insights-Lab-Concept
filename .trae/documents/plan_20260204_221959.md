I will replace the Mental Stress Load slider with a button group selection, similar to the Alcohol Type selector.

### Frontend Updates (`frontend/src/App.jsx`)

1.  **Replace Slider with Button Group**:
    *   In the "Recovery Foundation" section, remove the `ImpactSlider` for Mental Stress Load.
    *   Add a flex container with 3 buttons: "Low", "Medium", "High".
    *   **Styling**:
        *   Container: `flex justify-between gap-2` (similar to alcohol selector).
        *   Button Base: `flex-1 py-2 text-xs rounded-lg border`.
        *   Selected State: `bg-whoop-primary border-transparent text-white`.
        *   Unselected State: `border-whoop-textDim text-whoop-textDim hover:text-white`.
    *   **Label**: Add a label above the buttons, consistent with other inputs: `text-xs font-bold uppercase tracking-wider text-gray-400 mb-2`.

### Verification
*   The "Mental Stress Load" input will change from a slider to three distinct buttons.
*   The selected stress level (Low/Medium/High) will be clearly highlighted in the Whoop primary color.
