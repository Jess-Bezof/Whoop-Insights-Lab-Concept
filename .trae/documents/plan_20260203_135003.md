I will update the simulation logic to match the refined requirements, where Sleep Performance now includes penalties and Recovery is derived from that penalized score.

### Backend Updates (`backend/main.py`)

1. **Modify** **`/calculate`** **Endpoint Logic**:

   * **Calculate Sleep Performance ($S$)**:

     * Base: `(sleep / 8) * 100`.

     * Subtract Meal Penalty: `5 * max(0, 4 - meal_timing)`.

     * Subtract Stress Penalty: 10 (High), 5 (Med), 0 (Low).

     * Clamp between 0 and 100.

   * **Calculate Recovery ($R$)**:

     * Start with calculated $S$.

     * Subtract Systemic Stress Penalty: 15 (High), 5 (Med), 0 (Low).

     * Subtract Late Meal Penalty: `2 * max(0, 4 - meal_timing)`.

     * Clamp between 0 and 100.

### Frontend Updates (`frontend/src/App.jsx`)

1. **Verify/Update Display**: Ensure the two circular gauges are correctly labeled "Sleep" and "Recovery" and bind to the updated API response values.
2. **Clean Up**: Ensure the dashboard layout focuses on these two key metrics as requested.

### Verification

* I will run a curl command to verify the specific math (e.g., check if penalties are applied twice as specified).

