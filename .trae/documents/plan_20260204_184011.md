I will reorganize the dashboard sections as requested.

### Frontend Updates (`frontend/src/App.jsx`)

1.  **Refactor "Recovery Foundation" Section**:
    *   Remove "Meal Size" and "Meal Timing" sliders.
    *   Replace the "Mental Stress Load" buttons with a **visible slider** (`ImpactSlider`).
    *   Use the existing `getStressLabel` helper to show "Low", "Medium", "High" labels on the slider.

2.  **Create "Food Consumption" Section**:
    *   Add a new `DashboardCard` titled "Food Consumption" below the "Recovery Foundation" section.
    *   Move the "Meal Size (Calories)" and "Meal Timing" sliders into this new section.

### Verification
*   The UI will clearly separate Sleep/Stress (Recovery Foundation) from Food, Alcohol, and Caffeine.
*   The Mental Stress Load will now be controllable via a slider, consistent with other inputs.
