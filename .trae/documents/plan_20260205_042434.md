I will implement the redesign of the Daily Health Insights grid and the tiered diagnostic modals as follows:

1. **Backend Update (`backend/main.py`)**:

   * Update `calculate_recovery` to expose `caffeine_residue` in the response `details` object so it can be displayed in the UI (e.g., "85 mg").

2. **Frontend Logic (`frontend/src/App.jsx`)**:

   * Create a `DIAGNOSTIC_DATA` configuration object that maps each metric (`neuro_toxicity`, `metabolic_state`, `cns_stimulants`, `autonomic_load`) to:

     * **Units & Values:** (e.g., "units", "kcal", "mg", "ANS Score").

     * **Status Logic:** Map the backend status (`green`/`yellow`/`red`) to the user's specific text:

       * **Short Verdict:** (e.g., "✓ No CNS suppression" for the card pill).

       * **Modal Title:** (e.g., "NEURO-TOXICITY - PRIMED").

       * **Diagnostic Text:** The detailed explanations provided in your request.

   * Define a shared **Definitions** glossary (CNS, HRV, RHR, REM, etc.) to include in the modals.

3. **Frontend UI Redesign (`frontend/src/App.jsx`)**:

   * Refactor the 2x2 Grid to match the "WHOOP Health Monitor" style:

