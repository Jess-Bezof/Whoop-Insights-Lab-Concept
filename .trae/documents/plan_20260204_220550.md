I will increase the size of the metric rings as requested.

### Frontend Updates (`frontend/src/App.jsx`)

1.  **Ring Size**:
    *   Increase the `scale` factor for the central ring (Recovery) to make it larger.
    *   Update the `CircularMetric` component usage or styling to potentially increase the base size if needed, but the prompt says "make the rings the same size and larger".
    *   Actually, "make the rings the same size and larger" implies:
        *   Remove `transform scale-110` from the Recovery ring so it matches the Sleep ring? OR make both larger and equal?
        *   Interpretation: "same size" = equal dimensions. "and larger" = bigger than current base.
    *   Current: Sleep is base, Recovery is `scale-110`.
    *   Plan: Remove the scale difference. Increase the base `radius` in `CircularMetric` or use a prop.
    *   Wait, `CircularMetric` has hardcoded `radius = 36`. I should make this a prop or increase it.
    *   Let's update `CircularMetric.jsx` to accept a `size` or `radius` prop, or just increase the default if it fits.
    *   Better: Pass `size="large"` or similar. Or just increase the hardcoded radius to say 45 (90px width) -> 120px width?
    *   Let's check `CircularMetric` code again. It uses `w-24 h-24` (96px).
    *   I will update `CircularMetric` to accept `size` prop (default 96px).
    *   I will set both rings in `App.jsx` to be larger (e.g. `w-32 h-32` / 128px) and remove the `scale-110` wrapper to make them "same size".

### Component Update (`CircularMetric.jsx`)
*   Accept `className` to override width/height.
*   Update SVG `viewBox` and `radius` logic to be dynamic or just larger fixed size if easier.
*   Actually, SVG scales. If I change the container `w-24 h-24` to `w-32 h-32`, the SVG will scale up if `viewBox` is preserved.
*   So I just need to change the Tailwind classes passed to `CircularMetric` or in `App.jsx`.

### Plan
1.  **Modify `CircularMetric.jsx`**: Accept `size` prop (e.g., "lg", "xl") or just `className` to control dimensions.
    *   Let's add a `radius` prop default 36, but actually just scaling the container `div` is enough for SVG.
    *   I'll update it to allow `className` override.
2.  **Update `App.jsx`**:
    *   Pass `className="w-32 h-32"` (or larger) to both rings.
    *   Remove `<div className="transform scale-110">` wrapper from Recovery ring.

### Verification
*   Both rings will be equal size.
*   Both rings will be larger than before.

