I will implement the core simulation logic across the backend and frontend as requested.

### Backend Implementation (`backend/main.py`)
1.  **Create Data Model**: Define a `CalculationRequest` Pydantic model to accept `sleep_duration`, `stress_level`, and `late_meal_timing`.
2.  **Add Endpoint**: Implement a `POST /calculate` endpoint.
3.  **Implement Logic**:
    *   **Sleep Performance**: `(sleep_duration / 8) * 100`.
    *   **Stress Penalty**: Deduct 15 (High), 5 (Medium), or 0 (Low).
    *   **Meal Penalty**: For `late_meal_timing` < 4, deduct `(4 - late_meal_timing) * 5`.
    *   **Recovery Score**: `Sleep Performance - Stress Penalty - Meal Penalty`.
    *   Return both scores.

### Frontend Implementation (`frontend/src/App.jsx`)
1.  **State Management**: Add a state variable for `metrics` (Sleep & Recovery scores) initialized to defaults.
2.  **Real-time Sync**:
    *   Implement a `useEffect` hook that triggers whenever `sleep_duration`, `stress_level`, or `late_meal_timing` changes.
    *   Call the new `http://127.0.0.1:8000/calculate` endpoint in this hook.
3.  **UI Updates**:
    *   **Rings**: Update the top section to display exactly two rings: **Sleep Performance** and **Recovery**, binding them to the calculated `metrics`.
    *   **Input Grouping**: Group the Sleep, Stress, and Meal Timing sliders into a primary "Simulation Controls" card to satisfy the "all variables under one category" requirement.
    *   **Responsiveness**: Ensure slider movements immediately reflect in the rings.
