I will make the slider labels smaller and ensure the metric rings stick to the screen as the user scrolls.

### Frontend Updates

1.  **Modify `ImpactSlider.jsx`**:
    *   Reduce text size for the label (`text-sm` -> `text-xs`).
    *   Reduce text size for the value (`text-xl` -> `text-sm` or `text-base`).

2.  **Modify `App.jsx`**:
    *   **Sticky Rings**: Wrap the "Main Metrics Rings" section in a `sticky` container.
    *   Add `sticky top-0 z-10 bg-whoop-gradient/95 backdrop-blur-md py-4` to the rings container.
    *   This ensures they stay visible at the top while scrolling through the sliders below.

### Verification
*   Slider labels (e.g., "Sleep Duration") and values (e.g., "4.5h") will be smaller.
*   The rings will remain fixed at the top of the viewport when scrolling down.
