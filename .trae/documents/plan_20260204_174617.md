I will build the "Explainability Layer" by implementing the simulated physiological shifts in the backend and creating the corresponding UI in the frontend.

### Backend Updates (`backend/main.py`)

1. **Modify** **`/calculate`** **Endpoint**:
   * **HRV Shift**:
     * Base: 0%.
     * Subtract 15% if High Stress.
     * Subtract 12% per `active_alcohol_unit`.
   * **RHR Shift**:
     * Base: 0 bpm.
     * Add +5 bpm if meal < 2h before bed.
     * Add +4 bpm per `active_alcohol_unit`.
   * **Sleep Consistency**:
     * Default: "Optimal".
     * If `caffeine_residue > 100` or `active_alcohol_units > 2`: "Disrupted".
   * **Insight Generation**:
     * Logic to select the "highest impact" factor (Alcohol > Caffeine > Stress > Meal).
     * Return a string message.
   * **Return Object**: Add `health_metrics` dict with these 4 fields to the response.

### Frontend Updates (`frontend/src/App.jsx`)

1. **Health Monitor Grid**:
   * Create a new section below the rings.
   * **Card 1 (HRV)**: Display `%` change. Red if < -10%, Yellow < 0%, Green >= 0%.
   * **Card 2 (RHR)**: Display `+bpm`. Red if > 0, Green if <= 0.
   * **Card 3 (Restoration)**: Display Status text.
2. **Dynamic Insight**:
   * Add a text box at the bottom displaying the `insight` string from the backend.
3. **Design**:
   * Use existing `DashboardCard` or create a new styled component for these metrics to match the aesthetic.

### Verification

* I will verify with a test case:
  * High Stress, 2 Active Alcohol Units.
  * HRV: $-15 - (12 \times 2) = -39\%$.
  * RHR: $0 + (4 \times 2) = +8$ bpm.
  * Status: "Disrupted" (since active units 2 is not > 2... wait, logic says "> 2 units". If exactly 2, optimal? Let's check prompt: "alcohol > 2 units". Okay. Test with 2.1 units -> Disrupted).
  * Insight: Should mention Alcohol toxicity.

