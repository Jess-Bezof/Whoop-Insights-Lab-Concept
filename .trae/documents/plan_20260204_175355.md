I will replace the raw metric shifts with dynamic descriptive notifications for better explainability.

### Backend Updates (`backend/main.py`)

1. **Modify** **`/calculate`** **Endpoint**:
   * **Notifications List**: Initialize an empty list `notifications`.
   * **Alcohol (Active)**: If `active_units > 0`, append the "suppressing Parasympathetic" message. Tag as "Red".
   * **Alcohol (Metabolized)**: If `total > 0` and `active == 0`, append "residual inflammation" message. Tag as "Yellow".
   * **Caffeine**: If `residue > 100`, append "fragmented sleep" message. Tag as "Red" if > 150 else "Yellow"? Prompt says "Red: Caffeine > 150mg", "Yellow: Caffeine 50-150mg".
     * *Refinement*: If > 150: Red msg. If 50-150: Yellow msg.
   * **Late Meal**: If `hours < 2`, append "Late-night digestion" message. Tag as "Yellow".
   * **High Stress**: If "High", append "Fight or Flight" message. Tag as "Red". Medium Stress? Tag "Yellow".
   * **Optimization**: If list is empty, append "Optimal conditions" message. Tag as "Green".
   * **Return Object**: Include `notifications` list (each item has `message` and `color`).

### Frontend Updates (`frontend/src/App.jsx`)

1. **Remove Health Monitor Grid**: Remove the HRV/RHR/Restoration grid.
2. **Add "Daily Health Insights" Section**:
   * Iterate through the `notifications` list returned by the backend.
   * Render each as a "pill" or card.
   * **Styling**:
     * Red: `border-red-500 bg-red-900/20 text-red-200`.
     * Yellow: `border-yellow-500 bg-yellow-900/20 text-yellow-200`.
     * Green: `border-green-500 bg-green-900/20 text-green-200`.
     * Add a small status dot.

### Verification

* I will verify with a test case:
  * Active Alcohol > 0 (Red).
  * Caffeine 80mg (Yellow).
  * High Stress (Red).
  * Result: Should return 3 notifications with correct colors.

