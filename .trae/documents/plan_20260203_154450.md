I will implement the high-precision alcohol simulation logic across the backend and frontend.

### Backend Updates (`backend/main.py`)

1. **Modify** **`CalculationRequest`**:
   * Add `alcohol_volume_ml` (float), `alcohol_abv` (float).
2. **Modify** **`/calculate`** **Endpoint Logic**:
   * **Calculate Standard Drinks**: `(volume_ml * (abv / 100) * 0.789) / 14`.
   * **Calculate Exponential Penalty ($P_{alc}$)**: `12 * (standard_drinks ** 1.2)`.
   * **Update Sleep ($S$)**: Subtract `0.05 * standard_drinks` from the Quality Multiplier.
   * **Update Recovery ($R$)**: Subtract $P_{alc}$ from the final Recovery score.
   * **Return Details**: Include `standard_drinks` and `alcohol_penalty` in the response details.

### Frontend Updates (`frontend/src/App.jsx`)

1. **New Alcohol Section**:
   * Replace the simple "Alcohol Intake" slider with a detailed section.
   * **Drink Type Preset**: Buttons or dropdown for Beer (5%), Wine (12%), Spirit (40%). This sets the `abv` state.
   * **Volume (ml)**: Slider (0-1000ml).
   * **Count**: Slider or Stepper for "Number of Drinks" (Wait, user asked for "Number of Drinks" as a multiplier? The logic says "volume_ml" and "abv". Usually "Volume" implies per drink? Or total volume?
     * *Clarification*: "Volume (ml)" and "Number of Drinks".
     * *Interpretation*: Total Volume = `Volume per drink * Number of Drinks`.
     * *Wait, the prompt says*: "Volume (ml): (Slider from 0 to 1000ml)" and "Number of Drinks: (Simple multiplier)".
     * *Logic*: I will calculate `total_volume = volume_slider * count_slider`.
     * *Actually, simpler*: Just send `total_volume` to backend? Or send both? The backend just needs total volume or pre-calculated standard drinks.
     * *Decision*: I'll calculate `total_volume` in frontend and send it as `alcohol_volume_ml`. Or better, to follow the prompt "Accept volume_ml and abv_percentage", I'll send the *total* volume consumed.
     * *Wait, Standard Drink Formula*: `(volume_ml * (abv / 100) * 0.789) / 14`. If I have 3 beers of 330ml each. Total volume = 990ml.
     * *Frontend UI*:
       * Preset: Sets ABV.
       * Volume Slider: Sets volume *per drink* (e.g., 330ml, 500ml).
       * Count Slider: Sets number of drinks.
       * *Payload*: `alcohol_volume_ml = volume_per_drink * count`. `alcohol_abv = selected_abv`.
   * **Explainability**: Show "You are consuming {X} standard drinks..." text.

### Verification

* I will verify with a test case:
  * 2 drinks of 500ml Beer (5%).
  * Total Volume: 1000ml. ABV: 5%.
  * Standard Drinks: $(1000 \times 0.05 \times 0.789) / 14 = 39.45 / 14 \approx 2.81$.
  * $P_{alc} = 12 \times (2.81^{1.2}) \approx 12 \times 3.48 \approx 41.7$.
  * Sleep Penalty: $2.81 \times 0.05 \approx 0.14$.
  * Recovery Hit: -41.7 points.

