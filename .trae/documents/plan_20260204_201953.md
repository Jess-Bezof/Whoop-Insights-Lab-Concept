I will implement the "Educational Layer" with interactive modals for each section.

### Frontend Updates (`frontend/src/App.jsx` & Components)

1.  **Create `InfoModal` Component**:
    *   A reusable modal component that takes `isOpen`, `onClose`, `title`, and `content`.
    *   **Design**: Fixed positioning (centered), dark background (`bg-whoop-dark` or similar), high contrast text, close button, backdrop blur. Responsive sizing.

2.  **Define Educational Content**:
    *   Create a constant object mapping keys (e.g., "Sleep", "Recovery") to the provided text strings.

3.  **Update `App.jsx`**:
    *   **State**: Add `activeModal` state (string or null) to track which modal is open.
    *   **Interactive Titles**:
        *   Update the `CircularMetric` usage to wrap the label or make the label clickable.
        *   Update `DashboardCard` usage to accept an `onTitleClick` prop or similar, or just wrap the title in a button.
        *   Add hover effects (`hover:text-whoop-primary cursor-pointer`) to titles.
    *   **Render Modal**: Conditionally render `InfoModal` when `activeModal` is set.

4.  **Component Refactoring (Minor)**:
    *   If `DashboardCard` title rendering is internal, I might need to update `DashboardCard` to support the click handler and hover style. I'll check `DashboardCard.jsx` content.
    *   Same for `CircularMetric`.

### Verification
*   Clicking "Sleep" title opens the Sleep modal with correct text.
*   Clicking "Recovery Foundation" opens its modal.
*   Modals close via "X" button or backdrop click.
*   Responsive check: Ensure it looks good on mobile view (simulated).

