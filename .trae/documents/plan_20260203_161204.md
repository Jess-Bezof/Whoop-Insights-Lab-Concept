I will replace the alcohol calculation with the requested physiological clearance model.

### Backend Updates (`backend/main.py`)

1. **Modify** **`CalculationRequest`**:
   * Add `alcohol_hours_since_last_drink` (float).
2. **Modify** **`/calculate`** **Endpoint Logic**:
   * **Calculate Total Units**: Same standard drink formula.
   * **Metabolism**: `cleared = hours_since * 1.0`.
   * **Active Load**: `active = max(0, total - cleared)`.
   * **Recovery Penalty**:
     * $P_{active} = 8 \times (active^{1.1})$.
     * $P_{residual} = (total - active) \times 2$.
     * Total = $P_{active} + P_{residual}$.
   * **Sleep Penalty**: `0.05 * active` (subtracted from multiplier).
   * **Return Details**: Include `active_units`, `cleared_units`, and penalties.

### Frontend Updates (`frontend/src/App.jsx`)

1. **Update Alcohol Section**:
   * Replace "Number of Drinks" (or reuse it as multiplier?) -> User prompt says "Total Volume".
   * *Clarification*: The previous step had "Volume per Drink" and "Count". The prompt now says "Drink Type, Total Volume, Hours Since Last Drink".
   * *Implementation*: I'll keep the "Volume per Drink" and "Count" UI for better UX (easier to calculate total volume for user) but send the *Total Volume* to the backend.
   * **Add Slider**: "Hours Since Last Drink" (0-12h).
   * **Real-time Insight**: Display the "By the time you sleep..." message using the backend response data.

### Verification

* I will verify with a test case:
  * 3 Standard Drinks. 1 Hour since last drink.
  * Cleared: 1.0 unit.
  * Active: 2.0 units.
  * $P_{active} = 8 \times (2^{1.1}) \approx 8 \times 2.14 \approx 17.1$.
  * $P_{residual} = (3 - 2) \times 2 = 2$.
  * Total Recovery Hit = 19.1.
  * Sleep Hit = $2 \times 0.05 = 0.10$ (10%).

