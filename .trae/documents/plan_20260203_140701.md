I will decouple the Sleep and Recovery logic by implementing the weighted formula provided.

### Backend Updates (`backend/main.py`)

1. **Modify** **`/calculate`** **Endpoint**:

   * **Refined Sleep Performance ($S$)**:

     * **Quantity Score (70% weight)**: `(sleep / 8) * 100 * 0.7`.
       * *Wait, the formula says "Quantity Score" is `(hours / 8) * 100`, and "Quality" is 30 points max. Then S = Quantity + Quality?*
       * *Let's re-read carefully:* "Quantity (70% weight): (hours / 8) * 100" -> likely implies `(hours/8)*100 * 0.7`.
       * *Actually, usually "70% weight" means the final score is composed of weighted parts. Let's assume: Quantity Part = `(hours/8)*100 * 0.7`. Quality Part = `(QualityPoints/30) * 30`. Total = 100.*
       * *Correction based on text:* "Final S = Quantity Score + Quality Score".
       * *Interpretation*: Quantity Score = `(hours / 8) * 70` (max 70). Quality Score = Start at 30. Subtract penalties.
       * *Let's try that*:
         * Quantity Max = 70. Formula: `(hours / 8) * 70`.
         * Quality Max = 30. Start at 30. Subtract 5 * (4 - meal_time) and 15 for High Stress.
         * $S$ = Quantity + Quality.

   * **Refined Recovery ($R$)**:

     * **Nervous System Score ($N$)**: Start at 100.
       * Subtract 30 for High Stress.
       * Subtract 20 for meal < 2h before bed.
     * **Final $R$**: `(0.4 * S) + (0.6 * N)`.

### Verification

* I will verify the logic with a specific test case:
  * 8 hours sleep, High Stress, Meal at 1 hour.
  * Expected S: Quantity (70) + Quality (30 - 5*(3) - 15 = 0) = **70**.
  * Expected N: 100 - 30 (Stress) - 20 (Meal) = **50**.
  * Expected R: (0.4 * 70) + (0.6 * 50) = 28 + 30 = **58**.

