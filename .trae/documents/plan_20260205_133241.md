I will rebuild the 2x2 grid to match the "Dark Mode Monitor" aesthetic as requested.

1. **Dependency Management**:

   * Install `lucide-react` to provide the requested icons.

2. **Configuration (`tailwind.config.js`)**:

   * Add the specific hex codes for the new card style:

     * `monitor-bg`: `#121417`

     * `monitor-border`: `#22262B`

3. **Frontend Logic & UI (`frontend/src/App.jsx`)**:

   * **Import Icons**: Bring in `Brain`, `Zap`, `Coffee`, and `Activity` from `lucide-react`.

   * **Update** **`DIAGNOSTIC_DATA`**: Add an `icon` property to each metric configuration.

   * **Refactor Grid Render**:

     * **Container**: Use the new `bg-monitor-bg` and `border-monitor-border`.

     * **Header**: Flex row with the Lucide icon and uppercase label (text-gray-400).

     * **Body**: Large centered white text for the value + unit.

     * **Footer**: Conditional "Pill" with background colors based on status (Green/Yellow/Red) and the diagnostic verdict text.

     * **Interactivity**: Add `hover:border-slate-500` and ensure the whole card triggers the modal.

