I will make the selection border for Mental Stress Load and Alcohol Consumption buttons thinner.

### Frontend Updates (`frontend/src/App.jsx`)

1.  **Reduce Ring Width**:
    *   Change `ring-2` to `ring-1` for the active state of both "Mental Stress Load" and "Alcohol Consumption" buttons.
    *   This will create a thinner, more subtle highlight border while maintaining the emphasis.

### Verification
*   The white ring around selected buttons (Low/Medium/High, Beer/Wine/Spirit) will be 1px instead of 2px wide.
