I will make the metric rings significantly larger.

### Frontend Updates (`frontend/src/App.jsx`)

1.  **Increase Ring Size**:
    *   Change `size="w-32 h-32"` to `size="w-40 h-40"` (160px) or `w-48 h-48` (192px). Let's go with `w-44 h-44` (176px) for a noticeable increase without breaking layout on smaller screens.
    *   Since the previous update equalized them to `w-32 h-32` and the user wants "even larger", I'll bump it up substantially.

### Verification
*   The rings will be visibly larger than the previous 128px version.
