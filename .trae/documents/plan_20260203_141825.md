I will fix the "Zero-Sleep" floor issue and implement the new multiplicative logic for Sleep Performance.

### Backend Updates (`backend/main.py`)

1. **Modify** **`/calculate`** **Endpoint Logic**:

   * **Zero Rule**: Check `if data.sleep_duration == 0`: return 0 for sleep.

   * **Revised Sleep Performance ($S$)**:

     * **Quantity Score**: `(hours / 8)`.

     * **Quality Multiplier**:

       * Start at 1.0.

       * Subtract 0.05 \* (4 - meal\_time) (if < 4).

       * Subtract 0.15 (High Stress) or 0.05 (Medium Stress).

     * **Final $S$**: `Quantity * Quality * 100`.

   * **Revised Recovery ($R$)**:

     * **Nervous System Score**: Start at 100. Subtract 30 (High Stress), 20 (Meal < 2h).

     * **Final $R$**: `(0.4 * S) + (0.6 * N)`.

### Verification

* I will verify the logic with two test cases:

  1. **0 Hours Sleep**: Should return 0, 0 immediately.
  2. **8 Hours, High Stress, 2h Meal**:

     * Quantity: 1.0.

     * Quality: $1.0 - (2 \times 0.05) - 0.15 = 1.0 - 0.10 - 0.15 = 0.75$.

     * $S = 1.0 \times 0.75 \times 100 = 75$.

     * Nervous System: $100 - 30 - 0 = 70$ (Wait, meal is 2h, strictly "< 2h" penalty applies? Requirement says "meal < 2h". If exactly 2h, penalty is 0? Let's assume strict inequality as written).

     * $R = (0.4 \times 75) + (0.6 \times 70) = 30 + 42 = 72$.

