I will implement the caffeine simulation logic across the backend and frontend.

### Backend Updates (`backend/main.py`)

1. **Modify** **`CalculationRequest`**:
   * Add `caffeine_amount` (float) and `caffeine_timing` (float).
2. **Modify** **`/calculate`** **Endpoint Logic**:
   * **Calculate Residue**: `residue = intake * (0.5 ** (timing / 6))`.
   * **Calculate Caffeine Multiplier**: `1.0 - (residue / 10) * 0.01`. (e.g., 100mg -> 10 * 0.01 = 0.1 penalty -> 0.9 multiplier).
   * **Update Final S**: `Quantity * Quality * Caffeine_Multiplier * 100`.

### Frontend Updates (`frontend/src/App.jsx`)

1. **Update State**: Ensure `caffeine_amount` and `caffeine_timing` are part of the `inputs` state (they already seem to be from previous reads, but I will verify).
2. **Verify/Add Sliders**: Ensure the sliders for Caffeine Amount (0-600mg) and Timing (0-12h) are present and correctly wired to the `inputs` state and the calculation effect.
3. **Payload**: Ensure these new fields are sent in the POST request body.

### Verification

* I will verify the logic with a test case:
  * 200mg caffeine, 6 hours before bed.
  * Residue: $200 \times (0.5^{(6/6)}) = 200 \times 0.5 = 100$ mg.
  * Multiplier Penalty: $(100 / 10) \times 0.01 = 0.1$.
  * Multiplier: $1.0 - 0.1 = 0.9$.
  * Sleep Performance should be 90% of what it would be otherwise.

