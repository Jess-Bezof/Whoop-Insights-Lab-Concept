I will implement the requested UI changes to the Header and App components.

### Frontend Updates

1.  **Update `Header.jsx`**:
    *   **Remove "Today" and Arrows**: Delete the central navigation block (left arrow, "Today" pill, right arrow).
    *   **Remove Battery**: Delete the right-side battery percentage and icon.
    *   **Keep Profile Icon**: Keep the left-side profile icon (or user icon) as it wasn't explicitly asked to be removed, but the prompt says "remove Today and arrows next to it and battery on top". I will leave the left icon or just clean up the header to be minimal. The user instructions imply a cleaner header. I'll keep the left icon for balance unless "Insight Lab" title goes there.
    *   Actually, point 1 says "change name from Whoop to WHOOP Insight Lab". This is in `App.jsx`, not `Header.jsx`.
    *   Wait, the `Header` component currently has the arrows/battery.
    *   I'll clean `Header.jsx` to be very minimal or just remove the specific elements requested.

2.  **Update `App.jsx`**:
    *   **Change Title**: Update "WHOOP" to "WHOOP Insight Lab" in the main title section.
    *   **Remove "Simulate recovery impact"**: Delete the button/card at the bottom.
    *   **Emphasize Selection**:
        *   Update the "Mental Stress Load" and "Alcohol Consumption" button groups.
        *   Current selected style: `bg-whoop-primary border-transparent text-white`.
        *   New emphasized style: Add `ring-2 ring-white ring-offset-2 ring-offset-whoop-dark font-bold shadow-lg transform scale-105` to make it pop more. Or just a stronger background/border contrast. I'll use a stronger highlight effect.

### Verification
*   Header will be cleaner (no date/battery).
*   Title will read "WHOOP Insight Lab".
*   Bottom simulation button will be gone.
*   Selected buttons for Stress and Alcohol will have a more pronounced visual state.
