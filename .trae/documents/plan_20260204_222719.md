I will make the 2x2 Insight Grid cards interactive and display detailed educational content in modals.

### Frontend Updates (`frontend/src/App.jsx`)

1.  **Expand `EDUCATION_CONTENT`**:
    *   Add new entries for the 4 grid items: `Neuro-Toxicity`, `Metabolic State`, `CNS Stimulants`, `Autonomic Load`.
    *   Use the provided text, ensuring the title/content structure is handled (maybe just put the detailed text as the content, or handle title separately if the `InfoModal` supports it). The prompt provides "Title, Subtitle, Text". I'll format the text to include the subtitle or just use the text body.
    *   Let's map the keys to the provided content.
        *   `neuro_toxicity` -> "Alcohol & The CNS: Alcohol is a potent..."
        *   `metabolic_state` -> "The Cost of Digestion: Digesting food is..."
        *   `cns_stimulants` -> "Caffeine & Adenosine: CNS..."
        *   `autonomic_load` -> "Sympathetic Overdrive: Mental stress..."

2.  **Update Insight Grid Rendering**:
    *   Add `onClick` handler to the grid item `div`.
    *   Add hover styles: `cursor-pointer hover:scale-[1.02] transition-transform duration-200 hover:shadow-lg`.
    *   Wire up `openModal` with the appropriate key (e.g., "Neuro-Toxicity").

3.  **Ensure Modal Consistency**:
    *   The existing `InfoModal` should handle this fine. I will check if I need to pass a custom title vs using the key as title. The current implementation uses the key as the title.
    *   I might need to map the internal keys (`neuro_toxicity`) to display titles ("Neuro-Toxicity") for the modal header.

### Verification
*   Clicking any of the 4 cards opens a modal.
*   The modal displays the correct "Deep Dive" text.
*   The cards have a subtle scale/hover effect.

