I will refine the "Meal Before Bed" logic by incorporating caloric density and clearance rates.

### Backend Updates (`backend/main.py`)

1. **Modify** **`CalculationRequest`**:
   * Add `meal_calories` (float).
   * Note: `late_meal_timing` already exists, will reuse or rename to `hours_before_bed` for clarity (or just map it).
2. **Modify** **`/calculate`** **Endpoint Logic**:
   * **Clearance**: `processed = hours_before_bed * 200`.
   * **Active Load**: `active_calories = max(0, meal_calories - processed)`.
   * **Sleep Penalty**: `active_calories / 100 * 0.02` (2% per 100 cal).
   * **Recovery Penalty**: `active_calories / 50` (1 point per 50 cal). Subtract from ANS Score.
   * **Notifications**:
     * Update "Late Meal" notification logic to use `active_calories` instead of just timing.
     * Add the specific dynamic message about digestion.
   * **Return Details**: Include `active_calories` in response.

### Frontend Updates (`frontend/src/App.jsx`)

1. **Update Recovery Foundation Section**:
   * Replace the single "Late Meal Timing" slider.
   * Add **Meal Size (Calories)** slider (0-1500).
   * Add **Meal Timing** slider (0-6h?).
2. **Payload**: Ensure `meal_calories` is sent.
3. **Notifications**: Ensure the new dynamic message is displayed (it will come from the backend `notifications` list).

### Verification

* I will verify with a test case:
  * 800 cal meal, 2 hours before bed.
  * Processed: $2 \times 200 = 400$.
  * Active: $800 - 400 = 400$ cal.
  * Sleep Penalty: $(400 / 100) \times 0.02 = 0.08$ (8%).
  * Recovery Penalty: $400 / 50 = 8$ points.
  * Notification: Should warn about 400 active calories.

